name: 🚀 WINDOWS PRO ULTIMATE

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Duration'
        required: false
        default: '5h40m'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '5h40m'

jobs:
  windows-pro:
    runs-on: windows-latest
    timeout-minutes: 340
    
    steps:
    # Step 1: Initialize
    - name: 🏁 Initialize Windows Pro
      run: |
        echo "=============================================="
        echo "           WINDOWS PRO ULTIMATE"
        echo "=============================================="
        
        # Basic system info
        systeminfo | findstr /C:"OS Name" /C:"OS Version"
        echo "Initialization completed"

    # Step 2: Remove Test Mode
    - name: 🚫 Remove Test Mode
      run: |
        echo "=== REMOVING TEST MODE ==="
        reg delete "HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy" /v "VerifiedAndReputablePolicyState" /f 2>nul || echo "Test mode already removed"
        echo "Test mode removal completed"

    # Step 3: Performance Optimization
    - name: ⚡ Performance Optimization
      run: |
        echo "=== PERFORMANCE OPTIMIZATION ==="
        
        # Power plan
        powercfg -setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
        echo "High performance power plan activated"
        
        # Basic optimizations
        reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" /v "VisualFXSetting" /t REG_DWORD /d 2 /f
        echo "Performance optimization completed"

    # Step 4: Install Runtimes
    - name: 📦 Install Runtimes
      run: |
        echo "=== INSTALLING RUNTIMES ==="
        
        # Download and install VC++ Redist
        $vcUrl = "https://aka.ms/vs/17/release/vc_redist.x64.exe"
        $vcPath = "$env:TEMP\vc_redist.exe"
        try {
            Invoke-WebRequest -Uri $vcUrl -OutFile $vcPath
            Start-Process -FilePath $vcPath -ArgumentList "/install", "/quiet", "/norestart" -Wait -NoNewWindow
            Remove-Item $vcPath -Force
            echo "Visual C++ Runtime installed"
        } catch {
            echo "VC++ installation skipped"
        }

    # Step 5: Configure RDP
    - name: 🔐 Configure RDP
      run: |
        echo "=== CONFIGURING RDP ==="
        
        # Enable RDP
        reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server" /v "fDenyTSConnections" /t REG_DWORD /d 0 /f
        reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" /v "UserAuthentication" /t REG_DWORD /d 0 /f
        
        # Configure firewall
        netsh advfirewall firewall add rule name="RDP-Windows-Pro" dir=in action=allow protocol=TCP localport=3389
        
        # Start service
        sc config TermService start= auto
        net start TermService
        
        echo "RDP configuration completed"

    # Step 6: Create User Account
    - name: 👤 Create User
      run: |
        echo "=== CREATING USER ==="
        
        # Generate password
        Add-Type -AssemblyName System.Web
        $password = [System.Web.Security.Membership]::GeneratePassword(12, 3)
        
        # Create user
        net user WINPRO $password /add /y /expires:never
        net localgroup administrators WINPRO /add
        net localgroup "Remote Desktop Users" WINPRO /add
        
        # Save credentials
        echo "RDP_USER=WINPRO" >> $env:GITHUB_ENV
        echo "RDP_PASS=$password" >> $env:GITHUB_ENV
        echo $password > $env:TEMP\winpro_pass.txt
        
        echo "User WINPRO created successfully"

    # Step 7: Setup Tailscale
    - name: 🌐 Setup Tailscale
      env:
        TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
      run: |
        echo "=== SETUP TAILSCALE ==="
        
        if (-not $env:TAILSCALE_AUTH_KEY) {
            echo "TAILSCALE_AUTH_KEY not found in secrets"
            echo "TAILSCALE_IP=localhost" >> $env:GITHUB_ENV
            exit 0
        }
        
        try {
            # Download and install Tailscale
            $msiPath = "$env:TEMP\tailscale.msi"
            Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msiPath
            Start-Process msiexec.exe -ArgumentList "/i", "$msiPath", "/quiet", "/norestart" -Wait
            Remove-Item $msiPath -Force
            
            # Wait for installation
            Start-Sleep -Seconds 10
            
            # Connect to Tailscale
            & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_AUTH_KEY --hostname=win-pro-$env:GITHUB_RUN_ID
            
            # Get IP
            Start-Sleep -Seconds 5
            $ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
            if ($ip -and $ip -match '^\d+\.\d+\.\d+\.\d+$') {
                echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV
                echo "Tailscale connected: $ip"
            } else {
                echo "TAILSCALE_IP=win-pro.local" >> $env:GITHUB_ENV
                echo "Using hostname"
            }
        } catch {
            echo "Tailscale setup failed"
            echo "TAILSCALE_IP=localhost" >> $env:GITHUB_ENV
        }

    # Step 8: Display Info
    - name: 🎉 Display Connection Info
      run: |
        $password = Get-Content "$env:TEMP\winpro_pass.txt" -ErrorAction SilentlyContinue
        if (-not $password) { $password = "Check logs" }
        
        $duration = "${{ github.event.inputs.duration }}"
        $startTime = Get-Date
        switch ($duration) {
            "1h" { $endTime = $startTime.AddHours(1) }
            "3h" { $endTime = $startTime.AddHours(3) }
            "5h40m" { $endTime = $startTime.AddHours(5).AddMinutes(40) }
        }
        
        echo ""
        echo "=============================================="
        echo "           CONNECTION INFORMATION"
        echo "=============================================="
        echo "IP Address: $env:TAILSCALE_IP"
        echo "Username: WINPRO"
        echo "Password: $password"
        echo "Port: 3389"
        echo ""
        echo "Started: $($startTime.ToString('HH:mm:ss'))"
        echo "Ends: $($endTime.ToString('HH:mm:ss'))"
        echo "=============================================="

    # Step 9: Maintain Session
    - name: ⏳ Maintain Session
      run: |
        $duration = "${{ github.event.inputs.duration }}"
        switch ($duration) {
            "1h" { $minutes = 60 }
            "3h" { $minutes = 180 }
            "5h40m" { $minutes = 340 }
        }
        
        echo "Session active for $minutes minutes..."
        $endTime = (Get-Date).AddMinutes($minutes)
        
        do {
            $remaining = $endTime - (Get-Date)
            if ($remaining.TotalSeconds -le 0) { break }
            
            $hours = [math]::Floor($remaining.TotalHours)
            $mins = [math]::Floor($remaining.TotalMinutes % 60)
            $secs = [math]::Floor($remaining.TotalSeconds % 60)
            
            Write-Host "Time remaining: $($hours.ToString('00')):$($mins.ToString('00')):$($secs.ToString('00'))" -NoNewline
            Start-Sleep -Seconds 1
            Write-Host "`r" -NoNewline
            
        } while ($remaining.TotalSeconds -gt 0)
        
        echo "Session completed"

    # Step 10: Cleanup
    - name: 🧹 Cleanup
      if: always()
      run: |
        echo "=== CLEANUP ==="
        
        # Remove password file
        Remove-Item "$env:TEMP\winpro_pass.txt" -ErrorAction SilentlyContinue
        
        # Disconnect Tailscale
        & "C:\Program Files\Tailscale\tailscale.exe" down -ErrorAction SilentlyContinue
        
        # Disable RDP
        reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server" /v "fDenyTSConnections" /t REG_DWORD /d 1 /f
        
        echo "Cleanup completed"
